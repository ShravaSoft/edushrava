root = true

[*]
charset = utf-8
indent_size = 2
indent_style = space

# Force Unix-style newlines with a newline eding every file & trim trailing spaces
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
